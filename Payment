User
package prac􏰀ce;
public interface PaymentStrategy { public void pay(int amount);
}
import prac􏰀ce.PaymentStrategy;
public class CreditCardPayment implements PaymentStrategy{ private String cardNo;
public CreditCardPayment(String cardNo) { this.cardNo=cardNo;
}
System.out.println("Paid"+ amount +"using card number"+ cardNo);
}
public void pay(int amount) {

}
import prac􏰀ce.PaymentStrategy;
public class PaypalPayment implements PaymentStrategy{ private String email;
public PaypalPayment(String email) { this.email=email;
}
System.out.println("paid"+ amount +"using email id"+ email);
}
}
import prac􏰀ce.PaymentStrategy;
public void pay(int amount) {
